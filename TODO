+ stress test of parallel requests
- tests, refactoring

+ 2+ welcome messages
+ logs for bot
+ stats command
+ register new account and spin new instance
+ const name -> POLLING_INTERVAL
+ suffix -> tag
+ index for id
+ count field
+ history table
+ command for message broadcast


- move forced from Pinger to RemoteHost
- move forced from Pinger to listeners manipulation
- extract message handling into separate class, e.g. MessageDispatcher
- move models into a separate package
- logrotate
- FastAPI
- monitoring and logging